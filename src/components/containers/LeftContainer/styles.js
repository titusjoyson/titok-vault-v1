import { headerHeight } from '../../../theme/const';

const styles = {
    header:{
        height: headerHeight,
        display: "flex",
        fluxDirection: "row",
        justifyContent: "center",
        alignItems: "stretch",
    }
}

export default styles;